
  # Customer Support Ticket CRM

  This is a code bundle for Customer Support Ticket CRM. The original project is available at https://www.figma.com/design/NizyGl57eTde3ZnIF2679g/Customer-Support-Ticket-CRM.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  